# create constraints to append to the power models

function constraint_stability(pm::_PM.AbstractPowerModel, nw::Int, eigenVectorList::Array{Any,1})
    # create constraint generate by the eigenvalue analysis
end
